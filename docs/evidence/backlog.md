# Board — super-ux

The work-list between runs. Stage 0 reads it and quotes the open count;
stage 10 files every unresolved ledger row here with an id.

Priority is derived, not assigned: **Frequency × Severity × Solvability**,
the same scale the UX plans use. A row with no evidence column is a wish,
not a task.

Rows carrying an `SU-` prefix were raised by the cross-repository manifesto
conformance program; `B-` ids are this project's own series.

| id | Row | Priority | Source | Status |
|---|---|---|---|---|
| B-032 | **Closed 2026-08-31 in v0.52.0.** `test/evals/cases.json` carries four cases (`EV-01..EV-04`), each naming the instruction it measures by file and verbatim anchor; `test/evals/run.py` runs them through the `claude` CLI and refuses to report a pass when the CLI is absent. Deliberately outside CI: nondeterministic and paid, and a flaky required check is one people re-run until it passes. `validate_eval_cases` gates the part that IS deterministic — ids sequential, a brief and an expectation per case, and the anchor still resolving. Watched: rewording `**The humanization pass runs last**` in the skill turned `EV-03` red rather than leaving it quoting words nobody uses. Original row: **An instruction an agent reads has no gate, and this release added four of them.** `B064` checks that `voice.md` records a humanization state; nothing checks that the sweep actually ran, that the status line was printed, or that `Edit` ran it after the seven sweeps rather than before. The same is true of most of this pack: the linters verify artefacts, and the artefacts are downstream of behaviour nobody measures. What would close it is an eval harness rather than another linter code — a fixed brief run in a fresh context, with the output checked for the status line and for a marker density below the threshold the pass claims to enforce. The family already owns that ground in `agent-evals`, so the work is wiring rather than invention. Filed because R-76 and R-79 are `never` and the honest reason is that this class has no mechanism, not that these two rows were rushed. | 3×2×1 = 6| run 2026-08-30, v0.52.0| **resolved** |
| B-031 | **Closed 2026-08-31 in v0.52.0.** `AT-06`'s four grammatical exemptions carry ids `AT-06-E1..E4`, and `validate_ai_tell_coverage` now runs the arrow from a named exemption to a fixture asserting silence, in both directions. Watched: dropping the id from a fixture name → `FAIL: ai-tells.md declares the exemption AT-06-E4 and test/brand_lint_test.py has no fixture naming it`; renumbering the exemption → the same, for `AT-06-E9`. Original row: **A doctrine exemption is a promise the code need not keep.** `AT-06` has listed the no-value table-cell dash among its grammatical exemptions since it was written in v0.39.0, and `B062` reported that dash in every strict locale until v0.51.0 — sixteen days in which the pack shipped a rule its own linter contradicted, and the only reason it surfaced was a reader opening the function while widening it for a different reason. `B-017` closed the arrow from a **named marker** to a check; nothing runs the arrow from a **named exemption** to a negative fixture. All four of `AT-06`'s exemptions now have one — the copula, direct speech, the range in three spellings, the table cell — placed by hand, which is the same state `AT-` ids were in before `B-017`. Computable the same way: give the exemptions ids (`AT-06-E1..E4`), then run the both-directions comparison `validate_ai_tell_coverage` already runs, against fixture names rather than sections. | 2×3×2 = 12| run 2026-08-30, v0.51.0| **resolved** |
| B-030 | **Closed 2026-08-31 in v0.52.0.** `validate_audit_scope_enum` compares the four homes: the `argument-hint` and the step-1 enumeration must be equal, the `## … scope (`token`)` sections and the single-pass list must not stray outside them. Watched in both directions: `copy` removed from step 1 → `the user is offered a scope the agent does not enumerate`; removed from the hint → `the scope is legal and nothing offers it, which is SUX-06 exactly`. Original row: **The `/ux-audit` scope enum lives in three homes and nothing compares them.** The `argument-hint` in `commands/ux-audit.md`, the step-1 enumeration in `ux-audit/SKILL.md`, and the body's scope sections (`## Copy scope`, `## Benchmark scope`) each list the legal scopes independently. SUX-06 (family audit 2026-08-29) found `copy` and `benchmark:<competitor>` legal in the body and absent from both places an agent reads first — the drift is fixed in v0.50.0 by hand, and the class is not: a scope added to the body tomorrow goes invisible the same way. The check is computable — parse the hint's `[a \| b \| …]` list, the step-1 parenthetical, and the `## <Name> scope (\`token\`)` headings, and refuse a token any home lacks — same shape as `validate_status_enums_match_contract`. | 2×2×3 = 12| SUX-06, family audit 2026-08-29| **resolved** |
| B-028 | **Closed 2026-08-31 in v0.52.0.** A `Coverage:` citation may name its subject — `path:start-end symbolName`, inside the same backticks — and `U078` resolves the symbol and answers whether it is inside the span. It proves the subject is WITHIN the range, not that the range is exactly the subject, and that limit is stated in the code: matching a definition's extent needs a parser per language. This pack's own three citations now name `selectInteractive`, `selectFallback` and `installCursor`. Watched on the exact drift this row describes: `338-399` changed to `223-284` → `U078 … as covering `selectInteractive (found at line 338)`, which is not inside those lines`, while the bounds check alone stayed silent. Original row: **A `Coverage:` range proves its bounds and not its subject.** `U071`/`U072` resolve whether the cited lines exist, which caught `bin/super-ux.js:99000-99999` in a 396-line file — and cannot catch `223-284` in the same file once `selectInteractive` has moved to 235-296, which is exactly the drift that was live in `screens.md` for a release. Bounds are all a range can prove about itself. Making it checkable needs the citation to name its subject (`bin/super-ux.js:235-296 selectInteractive`), so the linter can re-find the symbol and compare — a contract change to `scenario-format.md`, a field the templates seed, and a resolver, filed rather than improvised. | 3×3×2 = 18| SU-04, 2026-08-20| **resolved** |
| B-029 | **Closed 2026-08-31 in v0.52.0.** The decision was the surface, as this row asked: `strings.md` gains a sixth column `Kind: copy | layout`. `layout` is a string whose shape carries the meaning — an aligned option table, a frame, a banner — registered so `B022` still knows it exists, and skipped by the rules that would be judging typesetting (`B010`/`B011`/`B012`, `B024`, `B026`, `B062`). `B065` refuses an out-of-enum kind; the enum joins the contract-parity gate. The two blocks are registered, and `python3 docs/brand/lint.py` now prints `brand pack is clean` where it printed a permanent warning. Watched: the skip removed → the layout fixture went red; the enum widened → both the fixture and the parity gate. Original row: **The usage option table is visible now and unregistered, and that is a standing warning.** Closing `B-003` made `usage()` readable to `B022`; the block's title is registered as `help.title` and its four `Usage:` lines are not, because a column-aligned table of flags is layout rather than language and its `Text` cell would carry a 34-space run. So `python3 docs/brand/lint.py` reports one warning on every run, forever, and `strings.md` records why. A permanent warning is a small tax on attention and it is the honest state of a help screen nobody has agreed; the alternative — a `_looks_like_copy` rule that drops any string with a three-space run — would also hide the two genuine copy decisions inside it (`interactive menu (multi-select)`, `Cursor rules, non-interactive`). Wants a decision about the surface, not a filter. | 1×2×3 = 6| SU-04, 2026-08-20| **resolved** |
| B-023 | **Closed 2026-08-31 in v0.52.0.** `cited_entries_date` answers the per-entry question with `git log -L` over each cited entry's own line span, and returns whether it could: on a renamed entry or without git it falls back to the whole file and `B005` says which question it answered. Measured on this repository: the whole file dates to 2026-08-20, `P-01,P-02` to 2026-08-10 — so a voice citing only personas no longer warns about a jobs edit, which is the false positive this row recorded from SU-02. Original row: **`B005` keys on a file where its own trace keys on entities.** `voice.md` declares `Derived-from: P-01, P-02, JTBD-01..03`, and `B004` checks exactly those ids — but `B005` compares `Last calibrated` against `content_date(foundation.md)`, so **any** edit anywhere in the file raises it. Measured on SU-02: seven story entries changed (`Acceptance:` → `Acceptance criteria:`, `Status: implemented` → `delivered`, a `Product:` line added), every hunk at line 99 or below the `## User stories` heading at line 93, and **zero** changed lines mentioning `P-0`, `JTBD-` or `JRN-` — yet the whole gate went red and a re-stamp was the only way to green. A warning that fires when nothing it protects moved is a warning people learn to re-stamp past, which is the opposite of what it is for. The fix is entity-level: compare the calibration date against the content date of the `Derived-from` **entries**, not of the file. `entry_blocks()` already isolates a persona or job body; what is missing is a per-entry date, which `git log -L` can give. | 3×2×2 = 12| SU-02, 2026-08-19| **resolved** |
| B-019 | **Closed 2026-08-31 in v0.52.0.** Not reproducible: four probes — each hard rule from each side of its pair — produced one line per defect and a count that matched. It was fixed by a refactor nobody credited. The class is now mechanical rather than absent: the report prints each distinct message once and names any duplicate emission, so lines and count cannot disagree again. Watched against a deliberately doubled `HARD_RULES`: `note: 1 duplicate emission(s); a check is firing twice for one defect`. Original row: **`validate_hard_rule_copies` prints one failure twice.** Observed on this run's own template edit: two identical `FAIL:` lines, summary counting `1 failure(s)`. Harmless to the verdict and corrosive to trust in the output, because a reader counting lines gets a different number from the one the gate reports. | 1×1×3 = 3| run 2026-08-14| **resolved** |
| B-022 | **Closed 2026-08-31 in v0.52.0.** The refresh ran unattended: `graphify . --update` twice plus `cluster-only`, 1149 nodes / 1801 edges / 159 communities, built from `6348b641`, $0.22 in tokens. `funnel-research.md` is in the graph for the first time, as are `landing-pages.md`, `onboarding.md`, `internal-screens.md` and `product-frameworks.md`. What the refresh surfaced is sharper than what this row asked: 58 label fields asserted `82 tags, 206 practices` about a catalog of 241 and an index stating no counts at all — a number no one computed, cached across refreshes. Corrected, and the class gated by `validate_graph_claims`, narrowed to `label`/`norm_label` because a node summarising a past defect legitimately quotes an old number. Original row: **The code graph is stale and the refresh has no unattended path here.** Built 2026-08-10 at 1206 nodes; its labels still say "206 practices" against a catalog of 215, and `funnel-research.md` appears in it zero times. `graphify . --update` on 2026-08-14 saw the change (`11 code, 52 docs changed`) and stopped at `error: no LLM API key found (52 doc/paper/image file(s) need semantic extraction)`; no key is present in this environment, and `graph.json` was left byte-identical, which `git status` confirms. `--code-only` needs no key and is the wrong trade for a repository that is mostly doctrine — it would answer fewer questions than the stale graph while looking current. The remaining path is the skill's fallback, where the host agent's subagents do the extraction, and it needs a run that is allowed to spawn them. Until then `GRAPH_REPORT.md` carries the staleness in its own header, because a stale graph is believed where a stale doc is argued with. | 2×3×2 = 12| run 2026-08-14, stage 9| **resolved** |
| B-021 | **Closed 2026-08-31 in v0.52.0, and it was already closed.** The arrow this row asked for — every `BP-NNN` reachable from a Step-2 or Step-3 row — has existed inside `validate_bp_index` since before the row was filed; planting a well-formed `BP-242` proved it (`are in the catalog but not routed -- no skill can reach them`). A duplicate check was written here and deleted rather than shipped. What was genuinely missing is the reverse: `practice-selection.md` routing at a `BP-NNN` the catalog does not define, invisible because every other check in that function starts from the catalog. Watched: `BP-999` planted into a routing row → `the row sends a profile at nothing`. Original row: **The selection protocol has no coverage gate, one level up from `B-017`.** `practice-selection.md` routes practices to profile facts and artifacts, and nothing asks whether every band in the catalog is reachable from some row. A practice added to the catalog and never routed is present and invisible, which is what `R-34` records as `never` for this run's five. The check is computable: every `BP-NNN` must appear in at least one Step-2 or Step-3 row, as a member or inside a range. | 3×2×3 = 18| run 2026-08-14, stage 10 ladder walk| **resolved** |
| B-001 | **Closed 2026-08-31 in v0.52.0.** `ux_lint.py` carries `VISION_RULE_TEXT`, and since that linter is seeded into every target project it is the only thing there that can hold the canonical rule. `U077` warns when the installed block differs. That makes a third copy, so it is compared: `validate_vision_rule_embed` refuses any drift from `templates/vision-rule.md`. Watched on both: a rule softened by hand in a target `CLAUDE.md` → `U077`; the embed edited → `ux_lint.py's VISION_RULE_TEXT differs from templates/vision-rule.md`. The scenarios hard rule has no target-project checker at all, which is a larger gap than this row and deliberately not folded into it. Original row: `check_vision` warns when a `vision.md` exists with no alignment rule, but nothing checks that the rule installed by the `vision` skill still matches `templates/vision-rule.md` **in a target project**. The repo gate covers the shipped pair only. | 2×3×2 = 12| audit 2026-08-10, stage 10| **resolved** |
| B-005 | **Closed 2026-08-31 in v0.52.0.** `U076` warns when every section of `vision.md` is a heading with nothing under it, so a seeded template says so instead of passing silently until someone self-declares `approved`. A warning rather than an error, because a new project legitimately starts there. The placeholder pass is the load-bearing half: section 9 ships three `<question>` lines, and counting those as content kept the check silent on the exact document it was written for — found by watching it not fire. Original row: `templates/vision.md` ships all nine headings with only comments beneath them. `read()` strips comments, so a project that seeds it and never writes a word lints clean until it is marked `approved`. The gate is status, and status is self-declared. | 2×2×3 = 12| dogfood 2026-08-10| **resolved** |
| B-025 | A journey has no owner field. `JRN-NN` records stages, actions, pains and opportunities; nothing names who owns an opportunity or the map itself, and a map with no named owner is updated once and then forgotten. Source: michaelbell.co.uk *What is a user journey* (2026), which cites NN/g and Salesforce. Needs a contract field, a template row and a linter check. | 2×2×3 = 12 | run 2026-08-10 | open · was `B-011` until SU-04 renumbered it (the 2026-08-10 brief cites the old id) |
| B-026 | The copy brief takes four inputs (action, reader, their words, proof) and no success metric, no constraint, and no reference to the voice's newly required `Admired`/`Refused` pair. A marketing piece with no stated metric cannot be reviewed against anything but taste. Also unfiled: the NN/g finding that a reader consumes ~20–28% of the words on a page, which is the evidence under "front-load the answer" and currently appears nowhere. Source: ameliepollak.com *How to hire a B2B SaaS copywriter* (2026). | 2×2×3 = 12 | run 2026-08-10 | open · was `B-012` until SU-04 renumbered it (the 2026-08-10 brief cites the old id) |

## Closed

| id | Row | Closed by |
|---|---|---|
| SU-03 | **Closed 2026-08-20.** Two defects that hid each other, closed in that order. `_entry_header_re` matches an entry by its **id** rather than by the colon after it, so the layer is visible to every rule in `ux_lint.py`; `U073` then refuses a header with no name and `U074` asks a job for `Statement`, `Personas`, `Type`, `Forces`, `Success metric`. This repo's three jobs carry all five, with metrics stated as user outcomes. Watched: two identical `### JTBD-01` headers passed the whole gate at `cc4c3eb`, exit 0; against the fix, `ERROR: [U001] foundation.md/jobs: duplicate id JTBD-01 (2 entries)`, exit 1. Eleven fixtures, three branch plants. See `verification.md` → R-56, R-57. | see `docs/evidence/verification.md` → 2026-08-20, SU-04 |
| B-024 | **Closed 2026-08-20.** `VOICE_STATUSES` is a module literal in `brand_lint.py`, `B034` refuses anything outside it, and `brand-contract.md` carries the declaration in the parseable one-home form that `validate_status_enums_match_contract` reads with `ast` — so the brand layer is inside the mechanism SU-02 built rather than beside it. `voice.md` moved to `validated`, which is the contract's own lifecycle sentence rather than a rename: `approved` is what the operator does, `validated` is what the file then says. R-58, R-59. | see `docs/evidence/verification.md` → 2026-08-20, SU-04 |
| B-016 | **Closed 2026-08-20.** `validate_board_ids` reads the first cell of every row in either table and refuses a repeat, and it asks the other direction too: a `B-` id cited in `verification.md` must resolve to exactly one row. The three open rows were renumbered `B-025`..`B-027`, the direction this row itself specified — the Closed rows are cited by `CHANGELOG.md:690,720`, a dated record this repository does not rewrite — and each renumbered row keeps a pointer to its old id. Watched: `B-023` renamed to `B-024` → exit 1; the ledger pointed at a three-digit id the board does not carry → exit 1. R-63. | see `docs/evidence/verification.md` → 2026-08-20, SU-04 |
| B-017 | **Closed 2026-08-20.** `validate_ai_tell_coverage` matches `\| AT-NN \|` rows against `### AT-NN.` sections in both directions and refuses a gap in the sequence. Watched, with the two mirrored copies re-synced so nothing else could take the credit: deleting the whole `### AT-15.` section while leaving its row kept `test/validate.py` at `OK (3539 checks)`, exit 0; against the check, `FAIL: ai-tells.md: AT-15 has a table row and no ### AT-15. section`, exit 1. R-62. | see `docs/evidence/verification.md` → 2026-08-20, SU-04 |
| B-018 | **Closed 2026-08-20**, together with `B-015` and by the same mechanism: `validate_hard_rule_anchors` measures anchor parity rather than byte equality, so a copy that diverges by choice is allowed to reword and not to drop a requirement. `CLAUDE.md`'s copy is covered with no exemptions. R-61. | see `docs/evidence/verification.md` → 2026-08-20, SU-04 |
| B-020 | **Closed 2026-08-20.** `R-22` now cites `B-017`, and it is no longer a `never`: the row records the plant, the exit codes and the fact that `B-016` was the id-reuse row. Done after the renumbering, which is the order this row asked for. R-62, R-63. | see `docs/evidence/verification.md` → 2026-08-20, SU-04 |
| B-003 | **Closed 2026-08-20.** It needed a second pattern, not a per-language extractor. `TEMPLATE_RE` reads a template literal and `code_literals` yields it a **paragraph** at a time — per line was tried first and turned wrapped prose into six findings no registry row could hold. `usage()` produced 16 findings on the first run, the top one `warn B022 bin/super-ux.js:0: "super-ux installer"`; that string is now registered as `help.title`, and `strings.md` records why the option table deliberately is not. R-66. | see `docs/evidence/verification.md` → 2026-08-20, SU-04 |
| B-004 | **Closed 2026-08-20**, and the live drift under it fixed. `coverage_claim` returns a third answer and `U071`/`U072` refuse a span the file does not have. Watched: `bin/super-ux.js:99000-99999` on `SCR-01` passed against a 396-line file, exit 0; against the fix, `ERROR: [U071]`, exit 1. All seven `screens.md` citations were pre-shift and were rewritten in both homes (14 in total), verified against `bin/super-ux.js` and against `scenarios.md`, which already had them right. **Bounds are all a range can prove about itself** — 284 is inside a 396-line file — so the residual limitation is filed as `B-028` rather than claimed closed. R-54, R-55. | see `docs/evidence/verification.md` → 2026-08-20, SU-04 |
| B-027 | **Closed 2026-08-20.** The umbrella was rewritten to the current system: `vision` above the chain, all seven rules in a routing table, the `Coverage:` range rule, and the brand layer with `python3 docs/brand/lint.py` beside the UX linter. R-60. | see `docs/evidence/verification.md` → 2026-08-20, SU-04 |
| B-014 | **Closed 2026-08-20.** `validate_skill_parity` now asks whether the always-on umbrella **names** each skill, and `validate_hard_rule_anchors` gives `.mdc` bodies their first gate. Watched: `copywriting` removed from the umbrella fires both at once, exit 1. R-60, R-61. | see `docs/evidence/verification.md` → 2026-08-20, SU-04 |
| B-015 | **Closed 2026-08-20.** Measured first: `templates/claude-rule.md` 320 words / 2102 chars, `CLAUDE.md:68` 349 / 2284, `cursor/rules/super-ux.mdc:6` 348 / 2204. Byte equality is the wrong test for a copy that diverges by choice and reads better for it, so `HARD_RULE_HOMES` compares **anchors** — every backticked and space-free bolded token the template section names — with exemptions carried as data and a reason. One exemption exists: `/ux` on the Cursor copy, because Cursor has no slash commands. The historical spec at `docs/evidence/specs/2026-07-19-super-ux-design.md:171` is a dated record and is not a live copy. R-61. | see `docs/evidence/verification.md` → 2026-08-20, SU-04 |
| SU-02 | **A shipped scenario silently counted as a validated one, and the field vocabulary was ungated.** Manifesto M-21: *a change can be implementation-verified and product-unvalidated; some outcome evidence cannot exist until after release, so `unobserved` is a legitimate product state, and pretending delivery proof is outcome proof is not.* The word `unobserved` appeared **zero** times in the pack (`git grep -l unobserved fe2189e \| wc -l` → 0) and `ux-audit/SKILL.md:211-212` flipped `validated` → `implemented` on an audit PASS — so the chain could record that the code does what the scenario said and had no way to record whether the scenario was the right thing to build. The second half, filed by SU-01: `scenario-format.md` declared `**Expected result:**`, `**Acceptance criteria:**` and the story statuses `proposed / validated / delivered / dropped`, while `docs/ux/scenarios.md` wrote `**Expected:**` on all 15, `docs/ux/foundation.md` wrote `**Acceptance:**` on all 7 and `**Status:** implemented` on all 7 — a value the story enum does not contain. `U060`/`U061` read both spellings, which is right for *their* question and left the vocabulary itself ungated. | `Product: unobserved / observed / contradicted` on scenarios and stories, with no floor and no target; `U066`..`U070` in `ux_lint.py`, 29 fixtures, six branch-keyed plants; `validate_status_enums_match_contract` and `validate_audit_leaves_product_alone` in `validate.py`. The long field spelling is canonical and `U069` says so; the audit writes the delivery state only, in both homes of the step. The pack's own chain swept: 15 scenarios and 7 stories, `unobserved` on all 22. Found on the way: the screens enum had drifted live — the contract declared five statuses and the linter matched four, so a `blocked` screen read as having no status. See `docs/evidence/verification.md` → 2026-08-19, R-42..R-50. |
| SU-01 | **The layer that DEFINES a requirement had no mechanism for the observable it demands.** Manifesto M-17 — a requirement with no observable is unfinished, because it cannot be connected to the evidence graph later without inventing the test after seeing the implementation — scored 5 of 18 across the family, and `super-ux`, which owns the intent graph, stated the rule in prose only. Measured: no rule in `ux_lint.py` opened a scenario or story body at all; `Expected result`, `Acceptance criteria` and a scenario `Coverage` returned nothing under grep. The screens layer below it had had its unfalsifiability check since `U055`/`U056`. | `U060`..`U065` in `ux_lint.py`, 20 fixtures, six branch-keyed plants, contract rows in `scenario-format.md`; the pack's own 15 scenarios now cite the code they were measured against, and the citations are re-resolved by the gate. See `docs/evidence/verification.md` → 2026-08-19, R-35..R-41. |
| B-006 | `graphify-out/graph.json` was stale at 756 nodes from 2026-08-06 — no `vision`, no new gates, no `docs/ux` or `docs/brand`. | Rebuilt 2026-08-10: **1091 nodes · 2111 edges · 71 communities** over 114 files. The recorded blocker — "needs an LLM key" — was a misread of the headless CLI path: with no `GEMINI_API_KEY`, semantic extraction falls to the host agent's own subagents, which is the documented default, not a degraded mode. Four subagents covered the 39 uncached docs; 59 came from the content-keyed cache. `--code-only` was never needed. Staleness block replaced by build notes in `GRAPH_REPORT.md`. |
| B-007 | **A public web surface had no home in the chain.** | Closed in v0.33.0. `screens.md` answers `Web surfaces: yes\|no` once, and a public screen carries a five-field `Web surface:` block — `Route`, `Answers`, `Indexable`, `Without JS`, `Entity` — each the design-time twin of a check `seo-aeo-audit` runs on the live page. `check_web_surface` in `ux_lint.py` with 14 fixtures; `ux-flows` asks, `ux-audit` verifies, `seo-aeo-audit` becomes the third companion. Contract stays v4. |
| B-008 | **The plain-word routing table was thinner than the capability behind it.** | Closed in v0.33.0. Four rows added to `commands/ux.md`: funnel/monetization/pricing/checkout, design/visuals/style, SEO/findability, mobile app/platform — each routing to the action and the practice set that already existed behind it. |
| B-009 | **A composite request mapped to one route.** | Closed in v0.33.0. Step 0 of `/ux` now maps every matching row, orders them by chain position, and states the sequence in one line before running the first. |
| B-002 | `validate_seeded_scripts` proved a command names both source and destination, not that the destination is the path the instruction tells the reader to run. | Closed in v0.34.0 by `validate_run_instructions`, which asks the other direction: every `python3 docs/**/*.py` named in a shipped instruction must be a destination some command seeds. Planted defect: an instruction renamed to `docs/ux/linter.py` — caught. |
| B-010 | `test/ux_lint_test.py` covered only `check_web_surface`; every check that predated it had no fixture. | Closed in v0.34.0. Rules got codes `U001..U054`, the harness covers all twenty-one with 43 checks, and `validate_ux_lint_coverage` requires a fixture and a contract row per code — so the harness cannot fall behind again. Three defects planted in the linter, each turned exactly one case red. |
| B-011 | ~~**The reference sweep in `ux-flows` step 2 has no verification.**~~ **Closed 2026-08-12** — run in a fresh context against a brief that never mentioned references; the agent swept before drawing and held both limits. R-14 is `observed`. Original row: It is doctrine an agent reads, not a script a gate runs, so `validate.py` can only confirm the words are present. Moving R-14 from `never` to `observed` needs one scenario in a fresh context with a reference server connected: brief a multi-step journey, watch whether the agent sweeps before drawing, and whether it holds both limits — a sweep may shape the journey but not decide the product's job, and never sets visual identity. Both Refero and Mobbin are authenticated on the author's machine as of 2026-08-12, so the blocker is a run, not access. *(was `high`; source: v0.35.0 reference sweep)* | **resolved** |
| B-012 | **Nine findings from the R-14 run — four actioned in v0.36.1, five open.** *Closed:* BP-123 now branches on who holds the billing (a store-billed flow cannot cancel and owes an *unverified* state) and requires a save offer to name its delivering mechanism; BP-102 and BP-206 cross-reference each other on the screen where the exit *is* the destructive action; the `flows.md` contract gains a `Rejected shape:` field so diverge-before-converging has somewhere to land; the Figma question's placement contradiction is resolved to step 0. **All nine closed as of v0.37.0.** The last five needed a decision rather than a correction, and in every one the fresh-context agent had already improvised the right answer under protest — so it was accepted rather than re-invented: a declared no-foundation branch (a provisional profile carrying each dimension's provenance, an unbacked provisional `Traces:`, an open-decisions list, and `Status: blocked` on the screens that depend on them); a granularity rule for the practice pass (per-practice for the artifact row and what it touches, one **reasoned** band verdict per remaining set); `blocked` added to the screen status enum; a state-vs-screen rule with a three-part test; and the platform-permits rule generalised out of BP-123 into the step where the diagram is drawn. *(was `high`; source: R-14 run, 2026-08-12)* | **resolved** |
| B-013 | ~~**No check-count ratchet.**~~ **Closed 2026-08-12 in v0.36.1.** `sheleg-design` has carried one since its retrospective recorded the class — a gate whose check count can fall silently cannot detect a deleted requirement; there, stripping four required headings dropped the count by one and the suite stayed green. This repository had none, so the same deletion here was invisible **by construction**, not by accident. Ported: `test/floors.json` plus `check_floor()` inside `validate.py`, which CI already runs. Watched saying no against a planted impossible floor — exit 1 with its own message. *(was `high`; source: ported class, 2026-08-12)* | **resolved** |
