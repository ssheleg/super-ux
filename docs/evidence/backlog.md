# Board — super-ux

The work-list between runs. Stage 0 reads it and quotes the open count;
stage 10 files every unresolved ledger row here with an id.

Priority is derived, not assigned: **Frequency × Severity × Solvability**,
the same scale the UX plans use. A row with no evidence column is a wish,
not a task.

| id | Row | Priority | Source | Status |
|---|---|---|---|---|
| B-016 | **The board reuses ids.** `B-011`, `B-012` and `B-013` each appear in *both* the open table and the Closed table below, carrying different content. A register whose ids are not unique cannot be cited: "closed in B-013" resolves to two rows. Nothing checks uniqueness, which is how three collisions accumulated unnoticed. Needs an id-uniqueness check in `validate.py` and a renumbering of the three open rows, in that order so the renumbering is verified by the thing that will keep it true. | 3×3×3 = 27 | run 2026-08-14, stage 0 harvest | open |
| B-017 | **The `AT-` marker set has ids but no coverage gate.** `ai-tells.md` now numbers every marker `AT-01`..`AT-15` precisely so coverage is computable, and nothing computes it. `validate_brand_lint_coverage` does this for `B0NN` (fixture + contract row per emitted code); the equivalent for `AT-NN` would assert every id in the table has a section below it and every section has a row above it. Until it exists, the ids are a promise rather than a mechanism, which is standing instruction #4 applied to this run's own artifact. | 2×2×3 = 12 | run 2026-08-14, stage 10 ladder walk | open |
| B-018 | **The repo's own `CLAUDE.md` copy of a hard rule is ungated.** `HARD_RULES` pairs `templates/claude-rule.md` with `commands/ux-rule.md` and compares the whole text, so the Brand voice section *is* covered there. The third copy, in this repository's `CLAUDE.md`, is not: it is abridged (1200 chars against the template's 1798 at the time of writing) and diverges by choice. Same class as `B-015`, and this run had to update it by hand with nothing to say whether the hand was right. | 3×2×2 = 12 | run 2026-08-14 | open |
| B-019 | **`validate_hard_rule_copies` prints one failure twice.** Observed on this run's own template edit: two identical `FAIL:` lines, summary counting `1 failure(s)`. Harmless to the verdict and corrosive to trust in the output, because a reader counting lines gets a different number from the one the gate reports. | 1×1×3 = 3 | run 2026-08-14 | open |
| B-022 | **The code graph is stale and the refresh has no unattended path here.** Built 2026-08-10 at 1206 nodes; its labels still say "206 practices" against a catalog of 215, and `funnel-research.md` appears in it zero times. `graphify . --update` on 2026-08-14 saw the change (`11 code, 52 docs changed`) and stopped at `error: no LLM API key found (52 doc/paper/image file(s) need semantic extraction)`; no key is present in this environment, and `graph.json` was left byte-identical, which `git status` confirms. `--code-only` needs no key and is the wrong trade for a repository that is mostly doctrine — it would answer fewer questions than the stale graph while looking current. The remaining path is the skill's fallback, where the host agent's subagents do the extraction, and it needs a run that is allowed to spawn them. Until then `GRAPH_REPORT.md` carries the staleness in its own header, because a stale graph is believed where a stale doc is argued with. | 2×3×2 = 12 | run 2026-08-14, stage 9 | open |
| B-020 | **`R-22` cites the wrong board id.** The verification ledger files the `AT-` coverage gap as `B-016`; the board files it as `B-017`, and `B-016` is the id-reuse row. One word, and it is a citation *into* the register whose id reuse is itself open above it, which is why it is filed rather than fixed in passing: renumbering per `B-016` will move the target again, and the two want to be done in that order. | 2×1×3 = 6 | run 2026-08-14, stage 0 harvest | open |
| B-021 | **The selection protocol has no coverage gate, one level up from `B-017`.** `practice-selection.md` routes practices to profile facts and artifacts, and nothing asks whether every band in the catalog is reachable from some row. A practice added to the catalog and never routed is present and invisible, which is what `R-34` records as `never` for this run's five. The check is computable: every `BP-NNN` must appear in at least one Step-2 or Step-3 row, as a member or inside a range. | 3×2×3 = 18 | run 2026-08-14, stage 10 ladder walk | open |
| B-001 | `check_vision` warns when a `vision.md` exists with no alignment rule, but nothing checks that the rule installed by the `vision` skill still matches `templates/vision-rule.md` **in a target project**. The repo gate covers the shipped pair only. | 2×3×2 = 12 | audit 2026-08-10, stage 10 | open |
| B-003 | The B022 literal extractor is a regex, not a tokenizer. It cannot see strings inside multi-line template literals, so `usage()` is invisible to the registry. Documented in `docs/brand/strings.md` → Not registered; a real fix needs a per-language extractor. | 3×2×1 = 6 | dogfood 2026-08-10 | open |
| B-004 | `docs/ux/screens.md` records coverage as `file:line` ranges that no check re-resolves. `ux_lint.py` verifies coverage exists, not that it lands. The same class as `B023` in the brand linter, which does re-resolve. | 3×2×2 = 12 | dogfood 2026-08-10 | open |
| B-005 | `templates/vision.md` ships all nine headings with only comments beneath them. `read()` strips comments, so a project that seeds it and never writes a word lints clean until it is marked `approved`. The gate is status, and status is self-declared. | 2×2×3 = 12 | dogfood 2026-08-10 | open |
| B-011 | A journey has no owner field. `JRN-NN` records stages, actions, pains and opportunities; nothing names who owns an opportunity or the map itself, and a map with no named owner is updated once and then forgotten. Source: michaelbell.co.uk *What is a user journey* (2026), which cites NN/g and Salesforce. Needs a contract field, a template row and a linter check. | 2×2×3 = 12 | run 2026-08-10 | open |
| B-012 | The copy brief takes four inputs (action, reader, their words, proof) and no success metric, no constraint, and no reference to the voice's newly required `Admired`/`Refused` pair. A marketing piece with no stated metric cannot be reviewed against anything but taste. Also unfiled: the NN/g finding that a reader consumes ~20–28% of the words on a page, which is the evidence under "front-load the answer" and currently appears nowhere. Source: ameliepollak.com *How to hire a B2B SaaS copywriter* (2026). | 2×2×3 = 12 | run 2026-08-10 | open |
| B-013 | **The Cursor umbrella rule ships a system three releases old.** `cursor/rules/super-ux.mdc:39` names four workflows (`ux-foundation`, `ux-flows`, `ux-scenarios`, `ux-audit`) and line 30 describes the chain as `foundation → flows → screens → scenarios`. Eight `.mdc` files ship. `vision`, `brand-voice` and `copywriting` — v0.30.0 and v0.31.0 — appear **zero** times in the umbrella, and the word `vision` appears in exactly one `.mdc` (its own). A Cursor user gets three rule files nothing routes to and a chain missing its top layer. | 3×3×3 = 27 | audit 2026-08-10 (Cursor axis) | open |
| B-014 | **Existence is checked, reachability is not — one channel over from B-008.** `validate_skill_parity` counts a `cursor/rules/<name>.mdc` as one of its five places, which is why `vision.mdc` exists; nothing asks whether the umbrella rule names it. And `validate_cursor_rules` reads front-matter only (`alwaysApply`, `description`), so `.mdc` **bodies** have no gate at all — which is the mechanism behind B-013 and behind the 0.21 drift before it. | 3×3×2 = 18 | audit 2026-08-10 (Cursor axis) | open |
| B-015 | **The `UX scenarios` hard rule has six homes; `HARD_RULES` pairs two.** The heading appears in `templates/claude-rule.md` (source), `commands/ux-rule.md` (gated), `cursor/rules/super-ux.mdc`, `commands/ux.md`, this project's own `CLAUDE.md`, and a historical spec. Two have measurably diverged: the `.mdc` (313 vs 341 words) and `CLAUDE.md` (13 differing lines — and its text is the *better* one, e.g. "the chain is designed and approved **for the change at hand**"). The v0.21 wiki entry predicted exactly this — "a summary of a contract is a second copy of it, and needs the same drift check as the shipped payload" — and produced no check. | 3×2×2 = 12 | audit 2026-08-10 (Cursor axis) | open |

## Closed

| id | Row | Closed by |
|---|---|---|
| B-006 | `graphify-out/graph.json` was stale at 756 nodes from 2026-08-06 — no `vision`, no new gates, no `docs/ux` or `docs/brand`. | Rebuilt 2026-08-10: **1091 nodes · 2111 edges · 71 communities** over 114 files. The recorded blocker — "needs an LLM key" — was a misread of the headless CLI path: with no `GEMINI_API_KEY`, semantic extraction falls to the host agent's own subagents, which is the documented default, not a degraded mode. Four subagents covered the 39 uncached docs; 59 came from the content-keyed cache. `--code-only` was never needed. Staleness block replaced by build notes in `GRAPH_REPORT.md`. |
| B-007 | **A public web surface had no home in the chain.** | Closed in v0.33.0. `screens.md` answers `Web surfaces: yes|no` once, and a public screen carries a five-field `Web surface:` block — `Route`, `Answers`, `Indexable`, `Without JS`, `Entity` — each the design-time twin of a check `seo-aeo-audit` runs on the live page. `check_web_surface` in `ux_lint.py` with 14 fixtures; `ux-flows` asks, `ux-audit` verifies, `seo-aeo-audit` becomes the third companion. Contract stays v4. |
| B-008 | **The plain-word routing table was thinner than the capability behind it.** | Closed in v0.33.0. Four rows added to `commands/ux.md`: funnel/monetization/pricing/checkout, design/visuals/style, SEO/findability, mobile app/platform — each routing to the action and the practice set that already existed behind it. |
| B-009 | **A composite request mapped to one route.** | Closed in v0.33.0. Step 0 of `/ux` now maps every matching row, orders them by chain position, and states the sequence in one line before running the first. |
| B-002 | `validate_seeded_scripts` proved a command names both source and destination, not that the destination is the path the instruction tells the reader to run. | Closed in v0.34.0 by `validate_run_instructions`, which asks the other direction: every `python3 docs/**/*.py` named in a shipped instruction must be a destination some command seeds. Planted defect: an instruction renamed to `docs/ux/linter.py` — caught. |
| B-010 | `test/ux_lint_test.py` covered only `check_web_surface`; every check that predated it had no fixture. | Closed in v0.34.0. Rules got codes `U001..U054`, the harness covers all twenty-one with 43 checks, and `validate_ux_lint_coverage` requires a fixture and a contract row per code — so the harness cannot fall behind again. Three defects planted in the linter, each turned exactly one case red. |
| B-011 | ~~**The reference sweep in `ux-flows` step 2 has no verification.**~~ **Closed 2026-08-12** — run in a fresh context against a brief that never mentioned references; the agent swept before drawing and held both limits. R-14 is `observed`. Original row: It is doctrine an agent reads, not a script a gate runs, so `validate.py` can only confirm the words are present. Moving R-14 from `never` to `observed` needs one scenario in a fresh context with a reference server connected: brief a multi-step journey, watch whether the agent sweeps before drawing, and whether it holds both limits — a sweep may shape the journey but not decide the product's job, and never sets visual identity. Both Refero and Mobbin are authenticated on the author's machine as of 2026-08-12, so the blocker is a run, not access. | v0.35.0 reference sweep | high | **resolved** |
| B-012 | **Nine findings from the R-14 run — four actioned in v0.36.1, five open.** *Closed:* BP-123 now branches on who holds the billing (a store-billed flow cannot cancel and owes an *unverified* state) and requires a save offer to name its delivering mechanism; BP-102 and BP-206 cross-reference each other on the screen where the exit *is* the destructive action; the `flows.md` contract gains a `Rejected shape:` field so diverge-before-converging has somewhere to land; the Figma question's placement contradiction is resolved to step 0. **All nine closed as of v0.37.0.** The last five needed a decision rather than a correction, and in every one the fresh-context agent had already improvised the right answer under protest — so it was accepted rather than re-invented: a declared no-foundation branch (a provisional profile carrying each dimension's provenance, an unbacked provisional `Traces:`, an open-decisions list, and `Status: blocked` on the screens that depend on them); a granularity rule for the practice pass (per-practice for the artifact row and what it touches, one **reasoned** band verdict per remaining set); `blocked` added to the screen status enum; a state-vs-screen rule with a three-part test; and the platform-permits rule generalised out of BP-123 into the step where the diagram is drawn. | R-14 run, 2026-08-12 | high | **resolved** |
| B-013 | ~~**No check-count ratchet.**~~ **Closed 2026-08-12 in v0.36.1.** `sheleg-design` has carried one since its retrospective recorded the class — a gate whose check count can fall silently cannot detect a deleted requirement; there, stripping four required headings dropped the count by one and the suite stayed green. This repository had none, so the same deletion here was invisible **by construction**, not by accident. Ported: `test/floors.json` plus `check_floor()` inside `validate.py`, which CI already runs. Watched saying no against a planted impossible floor — exit 1 with its own message. | ported class, 2026-08-12 | high | **resolved** |
